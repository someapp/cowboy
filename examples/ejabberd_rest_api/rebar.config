{require_otp_vsn, "R14|R15|R16"}.
{require_min_otp_vsn, "R14"}.

{cover_enabled, true}.

{edoc_opts,[{todo, true}]}.

{xref_checks, [undefined_function_calls, locals_not_used, exports_not_used]}.

{deps, [

    {reloader, ".*", {git, "https://github.com/oinksoft/reloader.git", 
"master"}},
		
	{ranch, ".*", {git, "git://github.com/extend/ranch.git", "0.6.2"}},
		
	{cowboy, ".*",
		{git, "git://github.com/extend/cowboy.git", "master"}},
	{mimetypes, ".*",
		{git, "git://github.com/spawngrid/mimetypes.git", "master"}}
]}.

{erl_opts, [
	     {i, ["../..", "../include", "../ejabberd-dev/trunk/include"]},
	     {src_dirs, [src]},
	     warn_format,
	     warn_export_all,
	     warn_export_vars,
	     warn_obselete_guard,
	     warn_unused_import,
	     warn_bif_clash,
	     warn_missing_spec,
	     warn_utyped_record,
         parse_transform, 
         debug_info]}.

{pre_hooks, []}.

{post_hooks, []}.

{sub_dirs, ["../presence_app/ebin", "deps", "include","rel"]}.

{reloader, ".*", {git, "https://github.com/oinksoft/reloader.git", 
"master"}}.

{escript_incl_apps, []}.




{eunit_opts, [verbose]}.
